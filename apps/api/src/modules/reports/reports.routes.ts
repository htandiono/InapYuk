import { Router } from 'express';

/**
 * Owner: Feature 2 - htandiono (Sprint 5)
 *
 * Planned endpoints:
 *   GET    /tenant/reports/sales      groupBy property | transaction | user
 *   GET    /tenant/reports/property   per-room availability calendar
 */
const router = Router();

export default router;
